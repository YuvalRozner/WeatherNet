specVersion: "v1beta"
serviceId: "weathernet"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "weathernet-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
