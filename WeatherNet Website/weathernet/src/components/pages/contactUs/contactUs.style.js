import styled from "styled-components";
import DialogActions from "@mui/material/DialogActions";
import DialogTitle from "@mui/material/DialogTitle";

export const DraggableTopArea = styled.div`
  cursor: move;
`;

export const DraggableDialogActions = styled(DialogActions)`
  cursor: move;
`;

export const DraggableDialogTitle = styled(DialogTitle)`
  cursor: move;
`;
